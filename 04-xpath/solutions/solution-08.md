# Solution 08

Bind a prefix in your XPath tool, map it to the document namespace URI, and use that prefix in the path expression. Do not query default-namespaced elements as if they were plain unnamespaced elements.
