# Common Mistakes

- Using `//` everywhere instead of learning specific paths
- Forgetting `@` when selecting attributes
- Comparing string values without checking the document structure first
- Ignoring namespaces in the query context
- Mixing up position-based filters and value-based filters
