# Solution 05

BaseX is an XML-native database and query engine. A team would use it when they want faster XML querying, better exploration tools, and support for XQuery and updates instead of manually treating XML as plain text.
