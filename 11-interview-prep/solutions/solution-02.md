# Solution 02

Well-formed XML follows syntax rules. Valid XML is well-formed XML that also matches a schema such as a DTD or XSD. The difference matters because valid XML gives downstream systems stronger guarantees.
