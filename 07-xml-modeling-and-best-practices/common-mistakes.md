# Common Mistakes

- Designing XML around one short-term query instead of the actual domain
- Hiding too much information in attributes
- Creating deeply nested structures that are hard to query
- Skipping validation even when the XML is exchanged across systems
- Using vague names like `item`, `data`, or `info` everywhere
