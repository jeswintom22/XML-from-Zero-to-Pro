# Exercise 05: Redesign A Poor XML Model

Take the bad example from this chapter and rewrite it into a cleaner XML structure with:

- a meaningful root
- clear element names
- separated values instead of one overloaded attribute
