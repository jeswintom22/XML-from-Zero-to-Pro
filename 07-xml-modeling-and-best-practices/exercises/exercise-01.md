# Exercise 01: Evaluate A Model

Compare [`../examples/good-model.xml`](../examples/good-model.xml) and [`../examples/bad-model.xml`](../examples/bad-model.xml). List three reasons the first is easier to work with.
