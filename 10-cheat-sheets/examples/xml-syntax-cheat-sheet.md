# XML Syntax Cheat Sheet

- One root element per document
- Close every opened tag
- Quote every attribute value
- XML is case-sensitive
- Escape special characters: `&lt;`, `&gt;`, `&amp;`, `&quot;`, `&apos;`
- Use comments as `<!-- comment -->`
- Use CDATA when you need raw text that contains markup-like characters
