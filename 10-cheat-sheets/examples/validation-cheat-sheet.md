# Validation Cheat Sheet

- Well-formed: syntax is correct
- Valid: syntax is correct and the document matches a schema
- DTD: simpler, older, not strongly typed
- XSD: XML-based, strongly typed, better for rich validation
- Validate early when downstream queries or transformations assume structure
