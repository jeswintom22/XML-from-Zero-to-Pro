# Exercise 05: DTD Or XSD?

Choose DTD or XSD for each case and explain why:

- a very old publishing pipeline with simple structure rules
- a product feed that needs numeric validation and date validation
- a small internal XML format that never uses namespaces
