# Exercise 01: Well-Formed Or Valid?

Classify each statement as true or false:

- Every valid XML document is well-formed
- Every well-formed XML document is valid
- DTD can describe element order
- XSD can define data types
