# Common Mistakes

- Calling a document valid just because it is well-formed
- Using DTD when strong data typing is required
- Forgetting that XSD is namespace-aware and written in XML syntax
- Missing required attributes defined in a DTD or XSD
- Writing schema rules that are stricter than the real data allows
