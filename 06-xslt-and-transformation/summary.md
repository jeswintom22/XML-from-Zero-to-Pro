# Summary

- XSLT transforms XML into HTML, XML, or text
- Template matching is the core mental model
- `apply-templates` keeps stylesheets modular
- Sorting, parameters, and grouping make reports more useful
- XSLT becomes most valuable when paired with realistic project data
