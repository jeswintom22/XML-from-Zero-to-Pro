# Solution 08

Hint:

- select each quarter node
- create a section per quarter
- inside it, render the KPI values you need
- if grouping rules become complex, design clear templates for period and KPI nodes
