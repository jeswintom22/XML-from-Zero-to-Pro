# Exercise 05: Find XML Around You

Find one real-world format or tool that uses XML behind the scenes. Write:

- the name of the format or tool
- what XML stores there
- why XML is useful for that case
