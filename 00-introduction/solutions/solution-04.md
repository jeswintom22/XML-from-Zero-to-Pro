# Solution 04

Possible differences:

- XML describes data, while HTML mainly describes how content is presented
- XML lets you create your own tag names, while HTML uses predefined tags
- XML is strict about closing tags and nesting, while HTML is more forgiving
