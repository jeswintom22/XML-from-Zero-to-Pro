# Solution 03

One simple tree:

- `company`
- `name`
- `location`
- `departments`
- `department`

The important idea is that `department` elements are children of `departments`, which is a child of `company`.
