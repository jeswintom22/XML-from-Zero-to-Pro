# Solution 07

Hint:

- iterate over `/university/students/student`
- capture the student id in a variable
- find matching enrollment records with the same student id
- return a `<student-roster>` element containing that student's courses
