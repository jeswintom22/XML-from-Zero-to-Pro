# Exercise 05: Prefix Or URI?

Answer true or false:

- Two different prefixes can point to the same namespace URI
- Changing a prefix always changes the meaning of the XML
- The namespace URI is what identifies the vocabulary
