# Summary

- Namespaces prevent name collisions across vocabularies
- A namespace is identified by its URI, not by its prefix text
- Default namespaces apply to unprefixed elements in scope
- Namespace-aware queries must bind prefixes explicitly
- The next chapter uses this idea directly in XPath
