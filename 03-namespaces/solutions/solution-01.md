# Solution 01

Namespaces prevent collisions when two XML vocabularies use the same element names. For example, one system may use `<name>` for a product and another may use `<name>` for a warehouse. Namespaces keep those meanings separate.
