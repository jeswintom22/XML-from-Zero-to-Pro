# Common Mistakes

- Forgetting to close a tag before closing its parent
- Using unquoted attribute values
- Mixing uppercase and lowercase tag names by accident
- Putting `&` directly inside text instead of using `&amp;`
- Using attributes for large blocks of content that should be elements
